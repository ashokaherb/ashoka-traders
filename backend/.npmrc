# multer-storage-cloudinary@4 declares a peer dependency on cloudinary v1, but works fine with
# the cloudinary v2 we use. Without this, a clean `npm install` fails with ERESOLVE - including
# the one Render runs on every deploy. Applies to every install in this folder (local + Render).
legacy-peer-deps=true
